# 霓虹心事 · Neon Heartbeats · 代码审查报告（m3）

> 项目根目录：`/Users/x/code/game1`
> 当前版本：v0.0.16（基于 `index.html` meta 标签、版本号注释与各模块的 `v0.0.x` 注释综合判断）
> 审查日期：2026-07-27
> 审查范围：项目全部源码 + 自动化测试 + 历史遗产 `legacy_avg` 目录

---

## 0. 项目速览

霓虹心事是一款「手机模拟器 + 多男主 AVG」玩法的网页剧情游戏：玩家扮演林夏（默认主角，可自定义），通过虚拟手机上的消息 / 电话 / 相册 / 朋友圈 / 出行等 App 来推进与三位男主（沈砚之 / 陆辞 / 江屿）和闺蜜苏苏的关系，最终走向不同结局。

整体技术栈非常轻量：**纯原生 JavaScript（ES2017+）+ DOM 操作 + localStorage 存档**，无任何前端框架、无构建系统、无 npm 依赖。测试用 `node:vm` 沙箱执行引擎脚本进行链路验证。

### 0.1 文件清单（主版本）

| 文件 | 行数 | 角色 |
|---|---|---|
| `index.html` | 666 | 单页结构：30+ 屏幕、状态栏、Toast、通知栈 |
| `style.css` | 1251 | 视觉系统：手机外壳、App 网格、动效、组件样式 |
| `engine.js` | 2193 | 核心引擎 `PhoneEngine`：时间、消息、电话、事件、存档 |
| `story.js` | 2367 | 数据层 `STORY`：角色、剧情、攻略系统、收藏、解谜等 |
| `app.js` | 3193 | UI 层：屏幕切换、App 渲染、事件绑定 |
| `test_story.js` | 1219 | 自动化剧情链路测试（vm 沙箱） |
| `legacy_avg/*` | 约 2290 | v0.0.3 早期 AVG 版本（线性对话框模式），已弃用 |
| `.gitignore` | 34 | 标准 ignore |

### 0.2 总规模

约 **12515** 行（含历史版本）。当前活跃版本（不含 `legacy_avg`）约 **10225** 行。

---

## 1. 整体架构

### 1.1 分层

```
┌────────────────────────────────────────────────────────────┐
│                       index.html  (结构层)                  │
│      · 30+ 个 <section class="screen"> 屏幕容器           │
│      · 顶部 status-bar / notif-stack / toast               │
│      · 通过 data-* 属性 + 委托绑定事件                      │
└────────────────────────────────────────────────────────────┘
                              ↓↑
┌────────────────────────────────────────────────────────────┐
│                        app.js  (UI/视图层)                  │
│   · IIFE 包裹 `(function(){...})()` 避免污染 window          │
│   · 单一 screens 字典缓存 DOM 引用                          │
│   · 一个 document.body 级 click 委托处理所有交互             │
│   · engine.on(...) 监听引擎事件 → 刷新 UI                    │
└────────────────────────────────────────────────────────────┘
                              ↓↑
┌────────────────────────────────────────────────────────────┐
│                    engine.js  (逻辑/状态层)                  │
│   · PhoneEngine 类：state 单源 + emit 事件 + 内部计时器     │
│   · 全部游戏逻辑：消息、电话、梦境、约会、存档、计算结局    │
│   · 通过 _setTimeout / _clearAllTimers 集中管理 setTimeout  │
└────────────────────────────────────────────────────────────┘
                              ↓↑
┌────────────────────────────────────────────────────────────┐
│                       story.js  (数据层)                    │
│   · STORY 常量：characters / events / dreams / locations … │
│   · 14+ 大型系统数据：商城、解谜、节日、视角、结局图鉴 …   │
│   · routeChoice / computeEnding / trueEndingUnlockCondition│
└────────────────────────────────────────────────────────────┘
```

### 1.2 单文件分析

| 文件 | 行数 | 关键复杂度 | 主要职责 |
|---|---|---|---|
| `engine.js` | 2193 | 高 | 单一 `PhoneEngine` 类，14+ 个子模块（消息、电话、约会、收藏、成就、塔罗、节日、闪回、攻略等） |
| `app.js` | 3193 | 中 | 一个 IIFE，26+ 个 render* 函数，2 个全局 click 委托 |
| `story.js` | 2367 | 中 | 13 个顶层 STORTY.* 配置（events/dreams/locations/invitations/shop/tarot/…），3 个全局函数（computeEnding/optionHints） |
| `index.html` | 666 | 低 | 30+ 个 `<section>` 屏幕，无任何模板引擎 |
| `style.css` | 1251 | 低 | 纯 CSS，无预处理器 |

**最大单文件 = 3193 行（app.js）**，建议控制在 2500 行内（下面有具体拆分建议）。

---

## 2. engine.js · 引擎核心（2193 行）

### 2.1 引擎优势

✅ **定时器集中管理**
- `_setTimeout` / `_clearAllTimers` 统一注册与清理，避免 newGame / load 后「幽灵消息」。
- 对外暴露 `_timers: Set`、`_callMissTimers: Map`，支持并发通话。

✅ **effect 应用归一化**
- `_applyEffects(effects)` 集中处理 `affection / affectionDetail / flags / personality`。
- 反向同步综合 affection（用于关系阶段、动态称谓），避免两套数值不一致。

✅ **存档版本化与字段兜底**
- `SAVE_VERSION = 1` + `load()` 时合并 `defaultState()`，确保旧存档可读取新字段。
- `affectionDetail` 按角色级逐项合并，避免旧存档某角色数据缺失。

✅ **事件系统多型支持**
- `scheduleEvent` 支持 16 种事件类型：`message_batch / call / photo_unlock / music_unlock / note_add / calendar_add / moment_post / dream / advance_time / advance_day / route_choice / ending / encounter / group_message_batch` 等。
- 链式 `then` + `thenEvent` 支持复杂剧情拓扑。

✅ **结局判定可计算**
- `_compute(state) => endingId` 在触发时根据 flags 动态计算。
- `STORY.computeEnding(state, route)` 综合三轴 + hiddenFlag → good/normal/bad。

✅ **测试钩子**
- `engine._setTimeout` 注册的计时器可在测试中被列举 / 验证。
- 事件命名清晰（`stateChange / timeChange / messageReceived / choicePrompt / ending / …`），便于测试断言。

### 2.2 引擎风险 / 问题

🔸 **state 对象过大（约 50+ 字段）**
- `defaultState()` 中集中定义了 50+ 字段（day/minute/conversations/affection/flags/affectionDetail/endingSeen/diary/tarotHistory/nightmaresSeen/…）。
- 字段命名风格不统一：部分 `camelCase`（`affectionDetail`），部分 `snake_case`（`_visited_set`、`_solo_hidden`、`_weather`）。
- **建议**：拆分为 `engine.state.time`、`engine.state.relationships`、`engine.state.inventory` 三个嵌套对象，便于维护与单元测试。

🔸 **`_pendingDate` 不持久化但分散在多处**
- `startDate` 写入 `engine._pendingDate`，`finishDate` / `cancelDate` 才清掉。
- 新人维护时易遗漏：如果引擎在 startDate 后 / finishDate 前被 load() 覆盖，`_pendingDate` 残留会卡死后续「这周约会」。
- **建议**：在 `load()` 时主动 `this._pendingDate = null`。

🔸 **`affectionDetail` 三轴与综合 affection 双向同步存在边界情况**
- v0.0.15 引入的折算规则（`closeness += delta*0.5, trust += delta*0.3, tension += delta*0.2`）+ 反向同步 `affection += sum/3`。
- 在「玩家不断刷同一选项」场景下，可能因 `Math.round` 累积导致综合分超过理论值。
- **建议**：在 `_applyEffects` 中加单元测试覆盖 100 次连点同一选项，确认数值稳定。

🔸 **`scheduleEvent` 中的 `type: 'ending'` 触发后只清理 `_timers`，不清理 `_callMissTimers`**
- `triggerEnding` 调用 `_clearAllTimers` 是 OK 的，但 `_callMissTimers` 是用 `setTimeout` 裸调用（不在 `_timers` 里），需要单独 clear。
- 已通过 `_clearAllTimers` 集中清理，✅。
- **但**：`addVoicemail` 等函数在 `triggerEnding` 之后被调用时会写入 `state.voicemails`，已加 `ended` 标记阻拦，✅。

🔸 **`_applyEffects` 在没有 affectionDetail 时旧逻辑会崩**
- `affectionDetail` 是 v0.0.15 后引入的字段，旧测试 / 旧存档没有该字段。
- `_applyEffects` 中已通过 `if(this.state.affectionDetail && this.state.affectionDetail[k])` 保护，✅。
- **但 `load()` 默认值**：
  ```js
  affectionDetail: (()=>{ ... 合并逻辑 ... })()
  ```
  对旧存档兼容良好，✅。

🔸 **`queueMessage` 中 `conv.finished` 标记使用情况**
- 在 `queueMessage` 中检查 `conv.finished` 直接 return，但全代码库没有设置 `finished = true` 的地方。
- 即「会话可能关闭」的设计意图存在，但未启用，🔸 死代码。

🔸 **大量 `try { ... } catch(_) {}` 静默吞错**
- 多处 `try { fn(); } catch(_) {}` 会让条件函数出错时无可见反馈。
- 例如 `checkIntel / checkInvitations / checkDailyTasks / checkAchievements / checkEasterEggs / checkFlashbacks / checkNightmare / checkHoliday / checkThemeUnlocks` 等都用了 try-catch。
- **建议**：至少在 dev 模式输出 `console.warn('[checkIntel]', id, e)` 便于调试。

🔸 **`_pendingEncounterThen` 与 `enc.then` 的优先级**
- `resolveEncounter`：`const thenEvt = enc.then || this._pendingEncounterThen;`
- 但 `scheduleEvent` 中 `encounter` 类型事件：`this._pendingEncounterThen = evt.then || null;`
- 逻辑闭环，但 `_pendingEncounterThen` 是「下一个」要触发的事件，若玩家在偶遇屏前触发两个 encounter 事件（如节日事件 + 邀约事件），第二个会覆盖第一个。
- **建议**：改为数组 `_pendingEncounterThens`，按队列处理。

🔸 **`STORY.computeEnding` 在 `route=null` 时直接 return null**
- `if(!route) return null;` —— UI 端用此判断「未进入任何路线」，但 `route` 设置后玩家也能 reset。
- **建议**：在 `resetGame / newGame` 中同步清空 `state.route = null`，目前依赖 `defaultState()` 默认值兜底，✅。

🔸 **`save` / `load` 性能与配额**
- `localStorage.setItem('neon_phone_saves', JSON.stringify(saves))` 每次保存全量写入。
- 配额约 5–10MB，若玩家创建多角色档案 + 长 diary + 多 moments，可能溢出。
- 已 `try/catch` 捕获 `setItem` 失败，✅，但未提供「清理旧存档」UI。
- **建议**：在 `设置` App 中增加「清理过期存档」入口。

---

## 3. story.js · 数据层（2367 行）

### 3.1 数据组织亮点

✅ **配置即文档**
- `STORY.events` 中每个事件对象用 `type + delay + messages + then` 完整描述，结构高度自描述。
- 三轴影响一站式：`effects: { affection, affectionDetail, flags, personality, thenEvent }`。

✅ **结局 / 攻略系统**
- `routeChoice.options[].thenEvent` 让路线选择与剧情无缝衔接。
- `_compute: s => 'shenyan_good' | 'shenyan_bad'` 让结局判定可编程，无需手写多个事件。

✅ **多档礼物偏好**
- `shop.preferences[charId][itemId]` = 0.5 / 1 / 1.5 / 2 四档，支持 `cocktail_set` 给 jiangyu 1.5 倍同时还要按物品细分（`'1.5b'`）的特殊逻辑。

✅ **节日系统 + 起始日期**
- `STORY.seasons.startDate = {month:7, date:15}` + `holidays['7-15']` 让「一年前的今天」类剧情可触发。

✅ **闪回 / 前传章节**
- `STORY.flashbacks` 中每个 `flashback` 有多个 `scenes`，最后一个 scene 有 `choice.options`，奖励可同时含 `photo + flag + affection`，设计饱满。

✅ **隐藏彩蛋 / 真结局**
- `easterEggs.cond` + `achievements.condition` + `trueEndingUnlockCondition` 三套机制叠加，引导多周目。

### 3.2 数据层风险

🔸 **events 顺序依赖脆弱**
- 例如 `bar_invitation` → `bar_invitation_msg` → `bar_invitation_msg_2` → `photo_neon_city` → `moment_luci_neon_evt` → `jiangyu_first_msg`。
- 每个事件的 `then` 都是字符串 id，若开发者重命名 id，需手动同步多处。
- **建议**：用 `engine.events.nextOf('bar_invitation')` 这种链式 API 替代裸字符串引用，或在 tests 里强制断言事件链完整。

🔸 **`_compute` 中的 flag 命名规则不统一**
- `__shenyan_end_judge._compute` 用 `s.flags.shenyan_brave / shenyan_resist / shenyan_awaken / shenyan_choice` 等。
- 部分 flag 用 `1/0`，部分用 `'confront' / 'endure'` 字符串，部分用 `true`，类型不一。
- **建议**：定义 `STORY.flagSchemas` 常量统一声明 flag 类型与含义。

🔸 **dreams 与 memories 重复语义**
- `dreams` 是「夜间睡眠时触发的抽象片段」，`memories` 是「看相册时触发的回忆杀」。
- 两者都有 `title/desc/options/personality/shard/meaning`，逻辑几乎一致。
- **建议**：抽取 `STORY.fragmentTemplates` 公共结构，`dreams/memories/flashbacks` 各自只声明 trigger 即可。

🔸 **id 命名混用 `snake_case` 与 `camelCase`**
- 人物：`shenyan/luci/jiangyu/susu` ✅ 一致。
- 事件：`intro_susu_3 / day2_morning / shenyan_test_choice` ✅ 一致。
- 收藏品：`postcard_neon / stamp_first / evidence_lyrics` ✅。
- 但内部字段：`mood_brave / solo_independent / _visited_set / _solo_hidden / shenyan_critical_3` 🔸。
- **建议**：把 `_` 前缀的字段集中到一个 `engine.state.internal` 命名空间，对外只暴露 `state.flags / state.coins` 等。

🔸 **成就 / 彩蛋 condition 是纯函数**
- `condition: s => Object.values(s.conversations).some(c=>c.messages.some(m=>m.from==='me'))`
- 大量使用 `Object.values / Object.keys / filter / some`，可读性 OK，但无法静态分析依赖。
- **建议**：抽到独立函数 + JSDoc 描述，方便测试与重构。

🔸 **谜题答案硬编码在数据里**
- `puzzle_shenyan_office.answer: '1402'` 与 `puzzle_jiangyu_song.answer: '1243'` 直接暴露在客户端。
- 玩家开 DevTools 即可看到。
- **建议**：将答案哈希后再比对（仍不绝对安全，但提升破解成本）；或在客户端只发送 attempts，后端校验（但本项目无后端）。
- 妥协方案：在 UI 上加「试错次数 + 软上限」+ 答错时降级提示。

🔸 **塔罗卡只有 16 张，逆位概率 35% 固定**
- `cards: { fool, magician, …, world }` 共 16 张（22 张大阿尔卡那中节选）。
- 概率写死：`Math.random() < 0.35`。
- **建议**：把概率移至 `STORY.tarot.config.reversedProb = 0.35`，便于调优。

🔸 **结局图鉴 4 档 × 多线 共 16 个 ending，触发条件分散**
- `STORY.endingGallery` 中声明了 16 个 ending，但真正会被触发的只有 `route + tier` 组合（GOOD/NORMAL/BAD/HIDDEN）。
- `computeEnding` 仅当 `_route + _hidden` 时返回 `hidden`，其余只看 `totalDim + tension`。
- **建议**：在 README 中明确「当前已实现的 ending 触发路径」，避免让玩家误以为某些 ending 可达成。

🔸 **`STORY.ambiences` 优先级硬编码**
- `priority = ['anxious','rainy','night','dusk','dawn','day']`
- 修改顺序需同步 UI 端 `applyAmbienceToBody` 的层叠方式。
- **建议**：把 priority 移到 `STORY.ambiences.priority` 字段。

---

## 4. app.js · UI 层（3193 行）

### 4.1 UI 优势

✅ **统一的 click 委托**
- `document.body.addEventListener('click', ...)` 集中处理所有交互，60+ 个 `e.target.closest('[data-xxx]')` 匹配。
- 优点：无需在每个动态生成元素上单独绑定监听器；缺点：新人理解成本略高。
- 可读性 OK，但分散在 200+ 行连续 if 中（`app.js:107-243`）。

✅ **`escapeHtml` + `nl2brEscaped` 转义**
- 几乎所有动态插入的内容都经过 `escapeHtml` 处理，XSS 风险低。✅
- 但 `phone-frame::before` 之类的 `style.css` 注入？无。

✅ **时间相关 UI 解耦**
- `engine.on('timeChange', updateTimeDisplay)` 单独绑定，UI 不感知时间逻辑。
- `engine.on('timeAdvance', ...)` / `'timeAdvanceEnd'` 提供动画钩子。

✅ **26 个 render* 函数职责清晰**
- 每个 App 对应一个 `render*` 函数，内部 `innerHTML = ...` 全量重渲染。
- 性能可接受（DOM 节点数 < 几百），但大量重绘可能掉帧。

### 4.2 UI 风险

🔸 **`document.body` 上有两层 click 委托**
- 主委托在 `app.js:107-243`；电话 tab 委托在 `app.js:1445-1470`；闪回 / 群聊分别有。
- 多个委托在事件冒泡阶段依次执行，**嵌套回调可能误触**。
- 例如：点击 `data-vm-play` 按钮时，若该按钮也包含 `data-action` 属性（虽然目前没有），会先被主委托匹配再被电话 tab 委托匹配，造成重复触发。
- **建议**：抽取一个 `delegate('click', handler, scope)` 工具函数，按 DOM 树层级注册，避免重复。

🔸 **`innerHTML` 全量重渲染**
- `renderMoments / renderShop / renderTasks / renderPlayer / renderAffection / renderThemes / renderEndings / renderDates / renderDreams / renderHints` 全部 `innerHTML = '...'`.
- 优点：实现简单；缺点：
  - 丢失焦点（如发朋友圈时输入到一半切换界面后回来，输入被清空，已通过 `openPostMoment` 显式清空）。
  - 频繁重绘可能闪屏（动画被中断）。
  - 节点数多时（朋友圈 + 多条 + 评论）滚动可能卡顿。
- **建议**：对长列表（朋友圈、相册、收集柜）用增量渲染 + 虚拟滚动，或至少在 `innerHTML` 后用 `requestAnimationFrame` 重新触发动画。

🔸 **`renderMoments` 中硬编码了 SVG `getMomentArt`**
- `app.js:1188-1214` 中只支持 `city` / `gallery` 两种 art。
- 而 `STORY.moments` 中使用了 `art:'gallery'` 和 `art:'city'` —— 数据与 UI 同步，✅。
- 但 `STORY.themes` 中 `wallpaper: 'stars' / 'waves' / 'sunset' / 'ink' / 'rose' / 'dark'` —— UI 端 `applyThemeToBody` 只写 CSS 变量，不生成 art。
- 这部分已通过 CSS 实现，✅。

🔸 **`renderPlayer` 中的性格问答展示逻辑**
- `app.js:2342-2347`：展示玩家已答的题目和选项。
- 但 `savePlayer` 中只在「首次作答」时才应用 effects，已答的不会重算。
- 若玩家答了 A → 答了 B → 回到 A，可能期望 personality 重算。
- **建议**：UI 上提供「重置性格问答」按钮。

🔸 **`openPerspectiveScene` 中标记 `perspectivesSeen` 但未 emit 事件**
- `handlePsceneOption` 中调用 `engine.markPerspectiveSceneSeen(...)`。
- 该方法内会 emit `stateChange` 和可能 emit `truthEndingUnlocked`，✅。
- 但 UI 端没有订阅 `truthEndingUnlocked` 来打开真相屏。
- 玩家必须主动进「男主视角 App → 全部看 → 点揭示按钮」。
- 这是设计选择（不让真相结局强制弹出），✅ 但要在 UX 上有引导。

🔸 **未持久化的 UI 状态**
- `selectedMomentArt / currentEncounter / currentFlashback / currentPerspectiveCharId / currentPsceneId / currentNightmare / currentPuzzleId / currentDateScene / playerEditState / timeoutTimer / giftSendItemId / giftSendCharId / collectionCurrentCat / shopCurrentTab` —— 这些都是模块级变量。
- 这些状态仅在内存，刷新页面会丢失（部分合理：草稿态）。
- 但 `playerEditState` 包含玩家已编辑但未保存的姓名 / 昵称 / 年龄 / 颜色 / 称谓 / 性格答案。
- 刷新后这些数据丢失但 `engine.state.player` 未更新，不一致。
- **建议**：玩家编辑模态打开时立即存到 `sessionStorage`，刷新后恢复。

🔸 **`shopCurrentTab` 初始值是 'store'，但页面 DOM 中只有 3 个 `.shop-tab`**
- `renderShop(tab)` 在未传参时使用 `shopCurrentTab = tab || shopCurrentTab`。
- 第一次进入时 tab='store'，✅。
- 但 `shopCurrentTab` 是模块级变量，重渲染时不会重置，✅ 持久。

🔸 **`renderTasks` 中「任务全部完成」后才递增 taskStreak**
- `engine.checkDailyTasks()` 在跨天时检查昨日 `every(t=>t.completed)`，✅。
- 但 `generateDailyTasks()` 在跨天时立即生成新任务，会覆盖 `tasks.completed` 状态。
- 已通过 `lastTaskDay !== day` 判定，✅。

🔸 **`applyThemeToBody` 仅写 3 个 CSS 变量**
- `document.body.style.setProperty('--bg', t.bg)`
- 但 `--bg` 在 CSS 中只用于 `html,body{background:#000}` 的覆盖。锁屏 / 主屏背景依赖 `--wallpaper-1`，已写入，✅。
- **注意**：CSS 文件中并未使用 `var(--bg)` 实际生效（只设置变量没引用），需要复查。

🔸 **`renderMoments` 中内嵌字符串拼接多**
- 每条动态的 HTML 拼接达 40+ 行，可读性差。
- **建议**：抽到 `formatMomentHtml(m, interactions, charComments)` 工具函数。

🔸 **`engine.emit('ending')` 后调用 `showScreen('ending')`，但用户可能还想继续探索**
- 缺少「结局后保留主屏入口」的逻辑。
- v0.0.15 的 `endingGallerySeen` + `endingGallery` 已经设计为二周目解锁，✅。
- 但 UI 没有直接提供「重新开始」按钮（仅结局屏有 `data-action="restart"`）。
- **建议**：在结局后保留主屏 + 添加「🎬 重玩」按钮。

---

## 5. index.html · 结构（666 行）

### 5.1 优势

✅ **纯声明式 + data-* 属性**
- 每个屏幕都有明确的 `data-app / data-back / data-conv / data-music / data-call / data-route / data-action` 等属性。
- 适合 click 委托，添加新屏幕成本低。

✅ **30+ 屏幕覆盖完整**
- 主屏 + 锁屏 + 27 个 App + 8 个 overlay 屏（来电、相册查看、梦境、偶遇、闪回、真结局、约会场景、朋友圈发送、梦魇、结局）。
- 几乎所有 engine 端功能都有 UI 入口。

### 5.2 风险

🔸 **「v0.0.16 新玩法」注释散落**
- `<!-- v0.0.13 主角自定义App -->`、`<!-- v0.0.13 关系阶段App -->`、`<!-- v0.0.13 每日任务App -->`、`<!-- v0.0.13 观赏模式App -->`、`<!-- v0.0.15 好感度深度App -->`、`<!-- v0.0.15 主题App -->`、`<!-- v0.0.15 结局图鉴App -->`、`<!-- v0.0.15 约会App -->`、`<!-- v0.0.16 梦魇 App -->`、`<!-- v0.0.16 智能提示设置 App -->`、`<!-- v0.0.16 男主朋友圈互动 UI -->`、`<!-- v0.0.16 梦魇触发模态 -->`。
- 这些都是开发注释，发布版本应去除。
- **建议**：发布构建脚本 strip 注释。

🔸 **`messages-app` 与 `chat-screen` 重复使用 `conv-list` / `chat-body` 等 id**
- `messages-app` 中 `<div class="conv-list" id="conv-list">`；`groups-app` 中 `<div class="conv-list" id="group-list">`。
- 类名相同、id 不同，✅。
- 但部分屏幕的 inner HTML 用 class 而非 id（如 `.app-grid`），与引擎 `$()` 仅按 id 取元素不一致。
- **建议**：统一用 id 标识可被引擎取到的容器元素。

🔸 **未使用语义化标签**
- 全部用 `<section>`、`<div>`、`<button>`，未用 `<main>` / `<nav>` / `<article>`。
- 对单页游戏影响小，但对屏幕阅读器 / SEO 不友好。
- 妥协：项目是单页应用，语义化要求低。

🔸 **`<meta name="version" content="v0.0.16">` 与 `?v=0.0.16` 缓存破坏**
- 每次升版本需要手动同步 4 处（meta + 3 个 script `?v=` + 1 个 css `?v=`），易遗漏。
- **建议**：构建时自动注入。

🔸 **没有错误边界 / 全局错误处理**
- 任意 JS 错误直接 console，未捕获时屏幕卡死。
- **建议**：增加 `window.addEventListener('error', ...)` 显示通用错误屏 + reload 按钮。

🔸 **`<meta name="viewport">` 锁缩放**
- `maximum-scale=1.0, user-scalable=no` —— 无障碍问题。
- 部分老年玩家 / 视障玩家无法放大查看。

---

## 6. style.css · 视觉（1251 行）

### 6.1 优势

✅ **设计语言统一**
- 主题色：`--accent #ff5fa8`（粉）、`--accent2 #7a5cff`（紫）、`--accent3 #39d6ff`（蓝）、`--green #4ade80`、`--red #f87171`。
- 字号梯度：`11px (mute) / 12px (dim) / 13px (body) / 15px (title) / 18px (modal) / 72px (lock time)`。
- 圆角梯度：`6px / 8px / 10px / 12px / 14px / 16px / 20px`（与 `box-shadow` 渐变统一）。

✅ **手机外壳设计感强**
- `#phone-frame` 用 perspective 1200px + 黑色阴影 + 灰边框模拟真机。
- `@media (max-width:480px)` 退化为全屏，✅ 响应式。

✅ **暗色 + 玻璃拟态**
- `.overlay-screen` 用 `backdrop-filter:blur(8px)` 模拟玻璃质感。
- 与 STORY.themes 的 gradient 配色一致。

✅ **动画克制**
- `screenIn 0.35s ease` / `notifIn 0.4s ease` / `bounce 2s ease-in-out infinite`，不滥用。

### 6.2 风险

🔸 **CSS 变量未充分利用**
- 定义了 `--serif / --sans / --ink / --ink-dim / --ink-mute / --accent / --accent2 / --accent3 / --bg / --bg2 / --panel / --panel-glass` 等。
- 但 `--bg / --bg2` 在 CSS 中几乎没有 `var(--bg)` 引用，定义但未用。
- `--ambience-overlay` 在 `applyAmbienceToBody` 中被设置，但 CSS 中没有 `::before` 引用（仅写入了 phoneFrame.style，CSS 选择器未生效）。
- **建议**：要么删除未使用变量，要么在 CSS 中实际使用。

🔸 **`#phone-frame::before` 未定义**
- `applyAmbienceToBody` 给 `phoneFrame.style.setProperty('--ambience-overlay', overlay)`，但 `style.css` 没有 `#phone-frame::before { background: var(--ambience-overlay); }`。
- 即「氛围层」实际**不会显示**。🔸 功能不生效！
- **验证**：查 `style.css` 第 26-36 行 `#phone-frame` 选择器，没有 `::before` 规则。
- **建议**：补充：
  ```css
  #phone-frame::before{
    content:"";position:fixed;inset:0;pointer-events:none;
    background:var(--ambience-overlay,transparent);transition:background .5s ease;
  }
  ```

🔸 **`overflow:hidden` 在 `html,body` 上禁止滚动**
- `html,body{width:100%;height:100%;overflow:hidden;...}`
- 单屏游戏合理，但设置 App / 玩家编辑 / 长列表无法整体滚动。
- 每个屏幕内部用 `flex:1;overflow-y:auto;` 解决，✅ 但 mobile Safari 滚动穿透需要小心。

🔸 **`box-sizing:border-box` + `-webkit-tap-highlight-color:transparent` 全局设置**
- ✅ 正确，但 `-webkit-tap-highlight-color:transparent` 会去掉所有点击反馈，建议改为仅在 `.app-icon` 等需要的地方显式禁用。

🔸 **没有 `prefers-color-scheme: dark / light` 处理**
- 已是暗色主题，不需要处理；✅。

🔸 **`backdrop-filter` 在低端 Android 设备性能差**
- 几乎所有 overlay 屏用了 backdrop-filter，可降级为半透明背景。

🔸 **`#dream-screen / #flashback-screen / #encounter-screen / #truth-ending-screen / #date-scene-screen / #post-moment-screen / #nightmare-screen / #ending-screen` 共用 `.overlay-screen` 样式**
- 大部分屏幕结构类似（dream-content / flashback-content / truth-content …）。
- 可抽到公共类，但当前已用 `screenIn` 动画 + 各自容器，差异大，统一 ROI 低。

🔸 **`font-family` 未覆盖宋体的字重**
- 仅 `font-family:"Songti SC","STSong",serif`，未指定 `font-weight:600` 时渲染可能模糊。
- macOS 上 PingFang SC / Songti SC 优先显示，Linux 上可能 fallback 到 Noto Serif CJK SC。

---

## 7. test_story.js · 测试（1219 行）

### 7.1 优势

✅ **无需浏览器即可跑**
- 用 `node:vm` 沙箱加载 `story.js + engine.js`，构造 `localStorage` mock，可独立运行。
- 测试运行：`node test_story.js`。

✅ **覆盖主线剧情**
- 23 个测试用例覆盖「序章 → 沈砚之线 GOOD END」全链路，包括：
  - 3 段梦境
  - 3 位男主首次消息 + 朋友圈
  - 江屿电话 + 《夏》解锁
  - 路线选择 → 沈砚之线
  - 南方出差 + 晚宴 + 觉醒选项
  - 朋友圈互动（点赞 / 评论 / 自发动态）

✅ **事件驱动测试模式**
- `eventLog: []` 收集引擎 emit 的事件，`wait(predicate, timeout)` 异步等待。
- 比断言状态更适合测试时序逻辑。

✅ **`doChoice / doAnyChoice / drainSusuIntel / doDream / doAnswerCall` 工具函数分层清晰**
- 专门处理「旁白 vs 群聊 vs 邀约」的 choice 过滤。

### 7.2 测试风险

🔸 **没有断言引擎内部状态的不变量**
- 比如 `_timers` 在 newGame 后必须为空；`affectionDetail` 与 `affection` 在特定场景下必须满足 `ad.tension <= affection * 0.2 + epsilon`。
- **建议**：增加单元测试覆盖纯函数：
  - `_applyEffects({affection: {shenyan: 3}})` → 三轴 + 综合分。
  - `computeEnding({...}, 'shenyan')` 各分支。
  - `pickAutoChoice(opts, 'affection')` 排序正确。

🔸 **`vm` 沙箱与浏览器差异**
- `crypto.randomUUID` 在 `addVoicemail` 中有 fallback（`Date.now()+'_'+Math.random().toString(36).slice(2)`），但浏览器原生支持，OK。
- `setTimeout` 在 node 中真实执行，OK。
- **但**：浏览器中 `Date.now()` 等无差异。
- **但**：`JSON.parse` 行为完全一致。

🔸 **没有测试 `load` / `save` 的存档兼容性**
- 测试只跑 `newGame`，没验证 `save()` → `load()` 后状态恢复。
- **建议**：增加 `save(slot).load(slot)` 往返测试，确保 50+ 字段全保留。

🔸 **没有测试 `v0.0.10+` 新玩法**
- 测试主体仅到 `v0.0.7`（共同邀约 / 群聊），`v0.0.10+` 的收藏 / 解谜 / 节日 / 男主视角 / 收藏品 / 节日 / 闪回 / `v0.0.13` 主角自定义 / `v0.0.13` 每日任务 / `v0.0.15` 约会 / `v0.0.16` 梦魇 / `v0.0.16` 智能提示均无覆盖。
- **建议**：补充测试覆盖：
  - `buyGift` + `giveGift` 全链路（shop → inventory → gifts）。
  - `attemptPuzzle` 答错 / 答对。
  - `collectItem` → `checkEasterEggs`。
  - `answerQuiz` → `setPlayer` → `getCharNickname`。
  - `startDate` → `finishDate` 跨周冷却。
  - `checkNightmare` → `resolveNightmare` → mood 更新。
  - `setShowOptionHints(true)` → `getOptionHint(opt)` 返回值。

🔸 **`drainSusuIntel` 在测试中作为「副作用清理」**
- 说明主线测试会被「苏苏情报 choice」干扰。
- 这暴露了一个**设计耦合**：苏苏情报和主线事件可能同帧挂起。
- **建议**：要么把苏苏情报放到独立线程（延后 1 天），要么在 `queueMessage` 中检测已有 pendingChoice 时合并显示。

---

## 8. legacy_avg · 历史版本（2290 行）

### 8.1 现状

`legacy_avg/` 完整保留了 v0.0.3 之前的「经典 AVG」版本：
- 标题画面 / 选项 / 立绘 / 背景图。
- 简单 `AVGEngine` 类（172 行）：`loadScene / getCurrentStep / renderCurrent / choose`。
- 4 个场景：`prologue_1 / shenyan_route_1 / luci_route_1 / jiangyu_route_1` + 4 个 ending。

### 8.2 风险

🔸 **未在 `.gitignore` 中排除**
- 当前仍在仓库中。
- ✅ 我倾向于保留：作为「版本演进档案」，未来重构 / 重制可参考。
- **但**：如果项目从 `legacy_avg/` 切到当前版本，应该在 README / 入口给出明确说明，避免新维护者困惑。
- 当前 `index.html` 直接引用 `engine.js / story.js / app.js`（无路径前缀），不会加载 `legacy_avg/`，✅。

🔸 **`legacy_avg/engine.js` 与主 `engine.js` 名字冲突**
- 若有人把 `legacy_avg/` 复制到根目录，会出现 `class AVGEngine` 与 `class PhoneEngine` 同名问题（其实不冲突，是不同类名）。
- ✅ 安全。

🔸 **`legacy_avg/story.js` 中也有 `STORY.scenes / STORY.endings / STORY.characters`**
- 同名字段复制，可能让人误以为是同一份数据。
- **建议**：在 `legacy_avg/story.js` 顶部加注释：「⚠️ 历史版本，不要修改，新版本请编辑根目录的 story.js」。

---

## 9. .gitignore（34 行）

### 9.1 评估

✅ **覆盖了常见类别**
- 系统文件（.DS_Store / Thumbs.db）
- 编辑器（.vscode / .idea）
- 截图下载
- 日志与临时
- 依赖（node_modules / .venv）
- 本地环境（.env / .env.local）

### 9.2 建议补充

```
# 构建产物
dist/
build/
*.min.js
*.min.css

# 备份与历史
*.bak
*.tmp
backup/
archive/

# 测试覆盖率
coverage/
.nyc_output/

# 数据库（如未来引入）
*.db
*.sqlite

# Docker（未来）
docker-compose.override.yml

# 本机存档（避免意外提交）
neon_phone_saves*.json
```

---

## 10. 总体评价

### 10.1 项目健康度

| 维度 | 评分 | 说明 |
|---|---|---|
| 代码组织 | ★★★★☆ | 三层分离清晰，数据层丰富 |
| 可维护性 | ★★★☆☆ | 单文件过长（app.js 3193 行）、state 字段过多（50+） |
| 可扩展性 | ★★★★☆ | 新增 App / 新增事件类型成本低，引擎 emit 模式扩展性强 |
| 可测试性 | ★★★☆☆ | 已有链路测试，但新玩法覆盖不全 |
| 性能 | ★★★★☆ | 单屏 DOM 节点少，事件委托高效 |
| 可访问性 | ★★☆☆☆ | viewport 锁缩放、缺 aria-*、暗色对比度可能不足 |
| 安全性 | ★★★☆☆ | XSS 防护到位（escapeHtml），谜题答案暴露在客户端 |
| 国际化 | ★★☆☆☆ | 纯中文，硬编码，无 i18n 框架 |
| 文档 | ★★☆☆☆ | 仅靠代码内注释，无 README / API 文档 |

### 10.2 最值得优先解决的问题

1. **`#phone-frame::before` 缺失 → 氛围层失效**（修复成本 5 分钟）
2. **测试覆盖率不足**（补 v0.0.10+ 测试，约 1-2 天）
3. **app.js 拆分**（按功能模块拆 4-6 个文件，约 1 天）
4. **`engine.state` 嵌套化 + flag 命名规范化**（约 1 天）
5. **谜题答案服务端化或加密**（需要后端 or hash，约 1 天）
6. **README / 开发者文档**（约半天）

### 10.3 最值得保留的设计

1. **emit 事件 + click 委托** —— 让 30+ App 屏幕的复杂度可控。
2. **`_setTimeout` / `_clearAllTimers` 定时器集中管理** —— 解决了「幽灵消息」经典 bug。
3. **数据驱动剧情（STORY.events）+ `_compute` 结局判定** —— 让作者无需写代码就能扩剧情。
4. **SAVE_VERSION + 字段兜底 load** —— 老存档向后兼容。
5. **三层架构（engine / app / story）** —— 解耦清晰，便于替换 UI 或移植到 React/Vue。

### 10.4 适合长期演进的方向

- **服务端化**：把谜题判定、存档同步、玩家排行放到 Node 后端。
- **Web Components**：把每个 App 改成 `<neon-app-moments>` 等自定义元素，便于复用。
- **PWA**：增加 manifest + service worker，支持离线玩。
- **数据可视化**：用 ECharts 画出「性格画像雷达图」「三轴好感曲线」。
- **多语言**：把所有剧情文本提取到 `STORY.i18n.zh-CN`，便于英文版。
- **TypeScript 重构**：把 state / events / photos 等定义成 interface，编译期捕获 bug。

---

## 11. 模块级细节清单

### 11.1 engine.js 子系统清单

| 子系统 | 行号区间 | 复杂度 | 备注 |
|---|---|---|---|
| 状态管理 + 事件总线 | 1-200 | 中 | `defaultState` 50+ 字段 |
| 时间系统 | 267-348 | 中 | 跨天 / 跨小时 / 跨分钟枚举 |
| 事件调度 `scheduleEvent` | 351-419 | 高 | 16 种 type |
| 消息系统 | 437-540 | 中 | typing / followup / 已读不回 |
| 路线选择 | 559-567 | 低 | |
| 电话系统 | 578-640 | 中 | 并发 missed 计时器 |
| 相册 / 音乐 / 备忘录 / 日历 | 643-680 | 低 | |
| 朋友圈 | 683-798 | 中 | |
| 梦境碎片 | 801-825 | 低 | |
| 性格画像 | 828-856 | 低 | |
| 出行 / 偶遇 | 859-926 | 中 | |
| 回忆杀 | 929-959 | 低 | |
| 苏苏情报 | 962-980 | 低 | |
| 共同邀约 | 983-1059 | 中 | |
| 多人聊天群 | 1062-1128 | 中 | |
| 语音信箱 | 1131-1162 | 低 | |
| 闪回 / 前传 | 1165-1206 | 低 | |
| 结局 | 1215-1223 | 低 | |
| 礼物商城 | 1226-1281 | 中 | |
| 心情 + 内心独白 | 1284-1315 | 低 | |
| 塔罗占卜 | 1318-1346 | 低 | |
| 成就系统 | 1349-1396 | 低 | |
| 收集柜 + 彩蛋 | 1399-1436 | 低 | |
| 解谜玩法 | 1439-1492 | 中 | |
| 季节 + 节日 | 1495-1536 | 低 | |
| 男主视角 | 1539-1580 | 中 | |
| 主角自定义 | 1583-1632 | 中 | |
| 关系阶段 | 1635-1676 | 中 | |
| 每日任务 | 1679-1762 | 中 | |
| 观赏模式 | 1765-1775 | 低 | |
| 三轴好感 | 1778-1802 | 低 | |
| 主题壁纸 | 1806-1852 | 中 | |
| 结局图鉴 | 1856-1874 | 中 | |
| 每日约会 | 1878-1948 | 中 | |
| 梦魇 | 1952-1987 | 中 | |
| 智能提示 | 1990-2000 | 低 | |
| 朋友圈评论 | 2003-2050 | 中 | |
| 动态背景氛围 | 2054-2064 | 低 | |
| 存档 | 2067-2190 | 高 | 字段兜底大块 |

### 11.2 app.js render* 函数清单

| 函数 | 行号 | 渲染对象 |
|---|---|---|
| `renderMap` | 283 | 出行 / 地图 |
| `showEncounter` / `closeEncounter` | 319/343 | 偶遇屏 |
| `renderConvList` | 349 | 消息会话列表 |
| `updateBadges / DreamsBadge / EndingsBadge / DatesBadge` | 384-445 | 主屏角标 |
| `openConversation / renderChatBody / appendMessage` | 448-505 | 单个会话 |
| `showNarratorChoice / showChatChoice` | 558-655 | 弹窗选项 |
| `startTimeout / handleFreeSend` | 657-713 | 限时回复 |
| `handleCallAction / runCallScript` | 737-805 | 来电 + 通话剧本 |
| `renderAlbum / viewPhoto` | 808-853 | 相册 |
| `getPhotoArt / getMomentArt` | 854-1214 | SVG 美术 |
| `renderCalendar` | 932 | 日程 |
| `renderMusic` | 966 | 音乐 |
| `renderNotes` | 998 | 备忘录 |
| `renderCallLog` | 1030 | 通话记录 |
| `showRouteChoiceModal / handleRouteChoice` | 1068-1100 | 路线选择 |
| `renderMoments / openPostMoment / selectMomentArt / publishMoment` | 1103-1256 | 朋友圈 |
| `renderProfile / renderInvitationsList` | 1329-1414 | 性格画像 |
| `renderVoicemails` | 1417 | 语音信箱 |
| `renderGroupList / openGroupChat / renderGroupChatBody / showGroupChoice` | 1473-1574 | 多人聊天群 |
| `renderContacts / openCharProfile` | 1615-1666 | 通讯录 |
| `showFlashback` | 1670 | 闪回章节 |
| `renderShop / handleBuyGift / openGiftSendModal / selectGiftChar / handleConfirmGift / closeGiftSendModal` | 1738-1870 | 礼物商城 |
| `renderMood / handleDiarySubmit` | 1877-1922 | 心情 + 日记 |
| `renderTarot / handleDrawTarot` | 1928-1972 | 塔罗占卜 |
| `renderAchievements` | 1975 | 成就 |
| `renderCollection` | 2031 | 收集柜 |
| `renderPuzzles / openPuzzleDetail / renderPuzzleDetail / handlePuzzleSubmit` | 2084-2156 | 线索本 |
| `renderCalendar2` | 2166 | 节日 |
| `renderPerspective / openPerspectiveScene / handlePsceneOption / showTruthEnding` | 2221-2312 | 男主视角 |
| `renderPlayer / openPlayerEdit / renderPlayerEdit / handlePlayerEditClick / savePlayer` | 2324-2512 | 主角自定义 |
| `renderRelations` | 2520 | 关系阶段 |
| `renderTasks / claimStreakReward` | 2587-2674 | 每日任务 |
| `renderWatch / toggleWatchMode / setWatchStrategy` | 2677-2722 | 观赏模式 |
| `renderAffection` | 2725 | 好感度深度 |
| `renderThemes / handleThemeClick / handleIconThemeClick / applyThemeToBody` | 2770-2841 | 主题壁纸 |
| `renderEndings` | 2844 | 结局图鉴 |
| `renderDates / openDateScene / closeDateScene / handleDateClick / handleFinishDate` | 2890-2988 | 每日约会 |
| `renderDreams / openNightmareScreen / closeNightmareScreen / handleNightmareOpt` | 3024-3084 | 梦魇 |
| `renderHints / toggleOptionHints` | 3087-3114 | 智能提示 |
| `handleMomentReply` | 3176 | 朋友圈回复 |
| `applyAmbienceToBody` | 3166 | 氛围 |
| `init` | 3185 | 启动 |

**总计 50+ render* / handle* 函数**，分布合理但文件过长。

### 11.3 story.js STORTY.* 配置清单

| 字段 | 行号 | 内容 |
|---|---|---|
| `characters` | 7-13 | 5 个角色 |
| `events` | 15-739 | 100+ 剧情事件（序章 / Day2 / Day3 / Opening / 4 条线 + 真结局） |
| `moments` | 742-808 | 4 条朋友圈模板 |
| `dreams` | 812-846 | 3 个梦境 |
| `photos` | 849-865 | 3 张照片 + 后续追加 |
| `music` | 868-881 | 2 首音乐 |
| `endings` | 884-899 | 7 个结局 |
| `routeChoice` | 903-911 | 4 个路线选项 |
| `locations` | 915-1023 | 5 个地点 + 偶遇 |
| `memories` | 1027-1058 | 3 个回忆 |
| `intel` | 1062-1078 | 3 条苏苏情报 + 回复事件 |
| `invitations` | 1117-1151 | 3 个共同邀约 + 接受/拒绝/错过事件 |
| `groups` | 1263-1271 | 1 个群聊「霓城小分队」 |
| `profiles` | 1318-1352 | 4 个角色社交主页 |
| `flashbacks` | 1356-1394 | 2 个闪回章节 |
| `shop` | 1401-1452 | 商城 + 8 件商品 + 3 男主喜好 |
| `moods` | 1455-1461 | 5 种心情 |
| `moodEffects` | 1463-1470 | 5 种心情的效果 |
| `tarot.cards` | 1473-1493 | 16 张塔罗 |
| `achievements` | 1496-1524 | 12 个成就 |
| `trueEndingUnlockCondition` | 1526-1530 | 真结局解锁 |
| `collectibles` | 1534-1549 | 15 件收藏品 |
| `easterEggs` | 1552-1564 | 8 个彩蛋 |
| `puzzles` | 1567-1617 | 3 个谜题 |
| `seasons` | 1620-1650 | 季节 + 节日 |
| `malePerspectives` | 1654-1814 | 3 男主视角 |
| `playerCustomization` | 1817-1882 | 主角自定义 + 性格问答 + 动态称谓 |
| `relationshipStages` | 1885-1905 | 关系阶段 |
| `criticalEvents` | 1908-1978 | 临界事件 |
| `dailyTasks` | 1981-2000 | 任务池 + 连胜奖励 |
| `watchMode` | 2003-2039 | 观赏模式策略 |
| `affectionDimensions` | 2045-2049 | 三轴定义 |
| `affectionDimStages` | 2051-2070 | 三轴阶段 |
| `themes` | 2073-2081 | 7 个壁纸主题 |
| `iconThemes` | 2083-2087 | 3 个图标主题 |
| `endingGallery` | 2091-2112 | 16 个结局图鉴 |
| `computeEnding` | 2114-2136 | 结局判定函数 |
| `dateScenes` | 2141-2184 | 每日约会场景 |
| `dateCooldownDays` | 2186 | 约会冷却 |
| `nightmares` | 2191-2247 | 2 个梦魇 |
| `optionHints` | 2252-2288 | 智能提示规则 |
| `momentComments` | 2293-2354 | 男主朋友圈评论 |
| `ambiences` | 2358-2365 | 动态背景氛围 |

**总计 38+ 个 STORY.* 字段**，数据驱动设计成熟。

---

## 12. 行动建议（按 ROI 排序）

### 12.1 必须立即修（影响功能）

| # | 问题 | 文件 | 修复成本 |
|---|---|---|---|
| 1 | `#phone-frame::before` 缺失导致氛围层不生效 | `style.css` | 5 分钟 |
| 2 | `_pendingDate` 在 `load()` 后未清空 | `engine.js` | 5 分钟 |
| 3 | README 缺失，新人无法快速了解项目 | `README.md` | 1 小时 |

### 12.2 短期修（提升可维护性）

| # | 问题 | 文件 | 修复成本 |
|---|---|---|---|
| 4 | 拆分 `app.js` 为 4-6 个模块 | `app.js` → `app/{ui,render,screens}/` | 1-2 天 |
| 5 | 补 v0.0.10+ 单元测试 | `test_story.js` + 新测试文件 | 1-2 天 |
| 6 | 抽离 `applyEffects` 三轴同步单测 | `engine.js` + `test_engine_unit.js` | 半天 |
| 7 | 整理 `STORY` 字段命名（flag / `_` 前缀） | `engine.js` + `story.js` | 半天 |
| 8 | 抽离 `engine.state` 为嵌套对象 | `engine.js` | 半天 |

### 12.3 中期规划（提升体验）

| # | 问题 | 文件 | 修复成本 |
|---|---|---|---|
| 9 | TypeScript 重构（先做 `.d.ts` 类型声明） | `*.d.ts` | 2-3 天 |
| 10 | PWA 化（manifest + service worker） | 新文件 | 1 天 |
| 11 | 增加玩家编辑草稿持久化（sessionStorage） | `app.js` | 半天 |
| 12 | 把谜题答案改为 SHA-256 哈希 | `engine.js` + `story.js` | 半天 |
| 13 | 提供「清理过期存档」UI | `app.js` | 2 小时 |
| 14 | aria-* 无障碍属性 + 移除 `user-scalable=no` | `index.html` | 1 天 |

### 12.4 长期演进

| # | 方向 | 收益 |
|---|---|---|
| 15 | 引入 Web Components，每个 App 独立打包 | 可复用 / 可嵌入其他项目 |
| 16 | 把「判定服务端化」（谜题 / 存档 / 排行） | 反作弊 / 跨设备 |
| 17 | 增加 i18n（先 zh-CN → en-US） | 海外玩家 |
| 18 | 增加剧情编辑器（可视化拖拽写 events） | 降低作者门槛 |

---

## 13. 总结

霓虹心事是一款**用代码当脚本编辑器**的剧情游戏：作者在 `story.js` 里写事件，作者在 `app.js` 里写 UI，作者在 `engine.js` 里写规则。

**架构清爽**：engine emit / app listen / story 数据三层分离。
**数据丰富**：38+ 个 STORY.* 字段覆盖了 90% 想要的玩法。
**测试到位**：1219 行链路测试覆盖了核心剧情路径。
**风格统一**：暗色 + 玻璃拟态 + 紫色主调，辨识度高。

**最大挑战**：
1. **app.js 单文件 3193 行** —— 建议按 App 拆分。
2. **`engine.state` 字段爆炸**（50+ 字段）—— 建议嵌套化 + 字段规范化。
3. **`#phone-frame::before` 缺失**（氛围层不生效）—— 5 分钟修复。
4. **新玩法测试覆盖不足**（v0.0.10+ 仅手动验证）—— 建议补充。
5. **`legacy_avg` 与主版本命名冲突** —— 建议加注释明确「历史版本」。

**若给一个工程化评分（10 分制）**：**7.5 / 10**
- 扣分项：单文件过长、state 字段扁平、氛围层 bug、测试覆盖不全、缺文档。
- 加分项：架构清晰、数据驱动、解耦优雅、风格统一。

整体是一个**架构良好、扩展性强、值得长期迭代**的独立游戏项目。

---

*报告生成于 2026-07-27，基于 `/Users/x/code/game1` 当前 HEAD。*